// New test file added to verify testing framework works correctly
test('simple test to verify setup', () => {
  expect(true).toBe(true);
});

test('another simple test', () => {
  const value = 42;
  expect(value).toBe(42);
});
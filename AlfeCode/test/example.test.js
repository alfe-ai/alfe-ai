// Example test file following the existing pattern
test('example test', () => {
  expect(2 + 2).toBe(4);
});

test('example test with string', () => {
  const text = 'Hello World';
  expect(text).toBe('Hello World');
});
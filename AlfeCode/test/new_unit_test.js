// Simple unit test added by assistant
const assert = require('assert');

describe('Simple arithmetic', function() {
  it('adds numbers correctly', function() {
    assert.strictEqual(2 + 3, 5);
  });
});

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sterling · OpenRouter :free Rate Limits</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap">
    <style>
        :root {
            color-scheme: dark;
            --nexum-bg-outer: #050713;
            --nexum-bg-inner: #111528;
            --nexum-panel: rgba(15, 20, 38, 0.85);
            --nexum-card: rgba(22, 28, 52, 0.92);
            --nexum-border: rgba(124, 144, 196, 0.22);
            --nexum-text: #e2e8f0;
            --nexum-muted: #94a3b8;
            --nexum-accent: #8a2be2;
            --nexum-accent-soft: rgba(138, 43, 226, 0.32);
            --nexum-accent-bright: #7dd3fc;
            --shadow-soft: 0 24px 48px rgba(7, 11, 25, 0.4);
        }
        * {
            box-sizing: border-box;
        }
        body {
            font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            margin: 0;
            padding: 52px 32px 72px;
            background: radial-gradient(circle at top left, rgba(138, 43, 226, 0.22), transparent 55%),
                radial-gradient(circle at 20% 120%, rgba(125, 211, 252, 0.22), transparent 55%),
                linear-gradient(135deg, var(--nexum-bg-outer), var(--nexum-bg-inner));
            color: var(--nexum-text);
            min-height: 100vh;
        }
        .page {
            max-width: 1080px;
            margin: 0 auto;
        }
        header {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-bottom: 32px;
            padding: 28px;
            border-radius: 24px;
            background: linear-gradient(145deg, rgba(17, 24, 39, 0.8), rgba(17, 24, 39, 0.6));
            border: 1px solid var(--nexum-border);
            box-shadow: var(--shadow-soft);
            backdrop-filter: blur(12px);
        }
        header h1 {
            font-size: 2.1rem;
            font-weight: 700;
            margin: 0;
            color: #f8fafc;
            letter-spacing: 0.02em;
        }
        header p {
            margin: 0;
            color: var(--nexum-muted);
            font-size: 1rem;
        }
        .nav-links {
            display: flex;
            flex-wrap: wrap;
            gap: 18px;
            margin-top: 8px;
        }
        .nav-links a {
            font-weight: 500;
            color: var(--nexum-accent-bright);
            text-decoration: none;
            position: relative;
            transition: color 0.25s ease;
        }
        .nav-links a::after {
            content: "";
            position: absolute;
            left: 0;
            bottom: -4px;
            width: 100%;
            height: 2px;
            background: linear-gradient(90deg, var(--nexum-accent), var(--nexum-accent-bright));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.25s ease;
        }
        .nav-links a:hover {
            color: #f8fafc;
        }
        .nav-links a:hover::after {
            transform: scaleX(1);
        }
        .section {
            margin-bottom: 26px;
            padding: 24px;
            border-radius: 20px;
            background: linear-gradient(135deg, rgba(17, 24, 39, 0.92), rgba(15, 23, 42, 0.78));
            border: 1px solid var(--nexum-border);
            box-shadow: var(--shadow-soft);
        }
        .section h2 {
            margin: 0 0 12px;
            font-size: 1.2rem;
            color: var(--nexum-accent-bright);
            letter-spacing: 0.05em;
            text-transform: uppercase;
        }
        .section h3 {
            margin: 18px 0 8px;
            font-size: 1rem;
            color: #f8fafc;
        }
        .section p,
        .section li {
            color: var(--nexum-text);
            line-height: 1.6;
        }
        .section ul {
            margin: 8px 0 0 18px;
            padding: 0;
        }
        .muted {
            color: var(--nexum-muted);
        }
        pre {
            background: rgba(8, 12, 26, 0.7);
            border: 1px solid rgba(125, 211, 252, 0.2);
            border-radius: 14px;
            padding: 18px;
            overflow-x: auto;
            color: #e2e8f0;
            font-size: 0.9rem;
        }
        code {
            font-family: 'JetBrains Mono', 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, monospace;
        }
        .callout {
            margin-top: 16px;
            padding: 16px 18px;
            border-radius: 16px;
            background: rgba(138, 43, 226, 0.14);
            border: 1px solid rgba(138, 43, 226, 0.32);
        }
    </style>
</head>
<body>
<div class="page">
    <header>
        <h1>OpenRouter :free Model Rate Limits</h1>
        <p>Read-only reference for checking rate limits and credits on OpenRouter keys.</p>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/openrouter/transactions">OpenRouter Transactions</a>
        </div>
    </header>

    <section class="section">
        <h2>API Guides</h2>
        <p>
            OpenRouter exposes a key inspection endpoint to view current credit limits and rate limit metadata.
            You can also retrieve remaining credits and daily usage for the key.
        </p>
        <h3>Rate Limits and Credits Remaining</h3>
        <p>
            Make a GET request to <code>https://openrouter.ai/api/v1/key</code> using your API key to get the
            latest limits and usage.
        </p>
        <pre><code>import { OpenRouter } from '@openrouter/sdk';

const openRouter = new OpenRouter({
  apiKey: '&lt;OPENROUTER_API_KEY&gt;',
});

const keyInfo = await openRouter.apiKeys.getCurrent();

console.log(keyInfo);</code></pre>
        <p class="muted">Expected response shape:</p>
        <pre><code>type Key = {
  data: {
    label: string;
    limit: number | null;
    limit_reset: string | null;
    limit_remaining: number | null;
    include_byok_in_limit: boolean;
    usage: number;
    usage_daily: number;
    usage_weekly: number;
    usage_monthly: number;
    byok_usage: number;
    byok_usage_daily: number;
    byok_usage_weekly: number;
    byok_usage_monthly: number;
    is_free_tier: boolean;
  };
};</code></pre>
    </section>

    <section class="section">
        <h2>Limits</h2>
        <p>
            OpenRouter applies different limits based on model type. Free model variants end in <code>:free</code>.
            Adding more API keys will not increase capacity because rate limits are global to the account.
        </p>
        <div class="callout">
            <h3>Free usage limits (:free models)</h3>
            <ul>
                <li><strong>20 requests per minute</strong> for any model ID ending in <code>:free</code>.</li>
                <li><strong>50 requests per day</strong> if you have purchased fewer than 10 credits.</li>
                <li><strong>1000 requests per day</strong> once you have purchased at least 10 credits.</li>
            </ul>
        </div>
        <p>
            Cloudflare DDoS protection may block requests that dramatically exceed reasonable usage.
        </p>
        <p>
            If your account has a negative credit balance, you may see <code>402</code> errors (including for free models).
            Adding credits to return above zero restores access to free tiers.
        </p>
    </section>
</div>
</body>
</html>

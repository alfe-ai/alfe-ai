<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Alfe AI - Add Repository</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap">
    <style>
        :root {
            color-scheme: dark;
            --nexum-bg-outer: #050713;
            --nexum-bg-inner: #111528;
            --nexum-panel: rgba(15, 20, 38, 0.85);
            --nexum-card: rgba(22, 28, 52, 0.92);
            --nexum-border: rgba(124, 144, 196, 0.22);
            --nexum-text: #e2e8f0;
            --nexum-muted: #94a3b8;
            --nexum-accent: #7c3aed;
            --nexum-accent-soft: rgba(124, 58, 237, 0.28);
            --nexum-accent-bright: #8b5cf6;
            --shadow-strong: 0 32px 68px rgba(3, 7, 18, 0.65);
            --shadow-soft: 0 24px 48px rgba(7, 11, 25, 0.4);
        }
        body {
            font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            margin: 0;
            padding: 48px 24px;
            background: linear-gradient(135deg, var(--nexum-bg-outer), var(--nexum-bg-inner));
            color: var(--nexum-text);
            min-height: 100vh;
        }
        .page {
            max-width: 860px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 24px;
        }
        header {
            padding: 24px;
            border-radius: 16px;
            background: linear-gradient(145deg, rgba(17,24,39,0.8), rgba(17,24,39,0.6));
            border: 1px solid var(--nexum-border);
            box-shadow: var(--shadow-soft);
        }
        header h1 {
            margin: 0 0 6px;
            font-size: 1.9rem;
        }
        header p {
            margin: 0;
            color: var(--nexum-muted);
            line-height: 1.5;
        }
        .card {
            padding: 24px;
            border-radius: 18px;
            background: linear-gradient(135deg, rgba(17, 24, 39, 0.92), rgba(15, 23, 42, 0.78));
            border: 1px solid var(--nexum-border);
            box-shadow: var(--shadow-strong);
        }
        .meta {
            margin: 0 0 18px;
            color: var(--nexum-muted);
            font-size: 0.95rem;
        }
        .meta-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
            flex-wrap: wrap;
            margin-bottom: 18px;
        }
        .meta-row .meta {
            margin: 0;
        }
        .meta-action {
            display: flex;
            align-items: center;
            gap: 8px;
            color: var(--nexum-muted);
            font-size: 0.95rem;
        }
        .alert {
            padding: 14px 16px;
            border-radius: 12px;
            border: 1px solid rgba(248, 113, 113, 0.4);
            background: rgba(127, 29, 29, 0.2);
            color: #fecaca;
            margin-bottom: 18px;
        }
        .alert.alert-ssh-warning {
            border-color: rgba(251, 146, 60, 0.5);
            background: rgba(251, 146, 60, 0.18);
            color: #fed7aa;
        }
        .ssh-card {
            border-radius: 14px;
            border: 1px solid rgba(124, 58, 237, 0.35);
        }
        .form-group {
            margin-bottom: 24px;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: var(--nexum-muted);
            font-size: 0.95rem;
        }
        .form-group input {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid var(--nexum-border);
            background: var(--nexum-card);
            color: var(--nexum-text);
            font-size: 1rem;
        }
        .form-group button {
            padding: 12px 24px;
            border-radius: 8px;
            border: none;
            background: var(--nexum-accent);
            color: white;
            font-size: 1rem;
            cursor: pointer;
        }
        .form-group button:hover {
            background: var(--nexum-accent-bright);
        }
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.5);
        }
        .modal-content {
            background-color: var(--nexum-bg-inner);
            margin: 15% auto;
            padding: 20px;
            border: 1px solid var(--nexum-border);
            width: 80%;
            max-width: 500px;
            border-radius: 10px;
            color: var(--nexum-text);
        }
        .modal-title {
            text-align: center;
            font-size: 1.5rem;
            margin-bottom: 20px;
        }
        .modal-body {
            margin-bottom: 20px;
        }
        .modal-footer {
            text-align: right;
        }
        .modal-footer button {
            margin-left: 10px;
        }
    </style>
</head>
<body>
    <div class="page">
        <header>
            <h1>Add Repository</h1>
            <p>Configure your new repository connection details</p>
        </header>
        <div class="card">
            <div class="meta">
                <div class="meta-row">
                    <div class="meta">Repository:</div>
                    <div class="meta-action">
                        <span>SSH Key:</span>
                        <span id="ssh-key-status">Not Set</span>
                    </div>
                </div>
            </div>
            <form id="repository-form">
                <div class="form-group">
                    <label for="repo-url">Repository URL</label>
                    <input type="text" id="repo-url" name="repo-url" placeholder="git@github.com:username/repo.git">
                </div>
                <div class="form-group">
                    <label for="repo-name">Repository Name</label>
                    <input type="text" id="repo-name" name="repo-name" placeholder="project-name">
                </div>
                <div class="form-group">
                    <button type="submit">Add Repository</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Modal for authentication -->
    <div id="auth-modal" class="modal">
        <div class="modal-content">
            <div class="modal-title" id="modal-title">Sign Up / Log In</div>
            <div class="modal-body">
                <form id="auth-form">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group" id="password-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password">
                    </div>
                    <div class="form-group" id="confirm-password-group" style="display: none;">
                        <label for="confirm-password">Confirm Password</label>
                        <input type="password" id="confirm-password" name="confirm-password">
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button id="modal-cancel">Cancel</button>
                <button id="modal-submit">Submit</button>
            </div>
        </div>
    </div>

    <script>
        document.getElementById('repository-form').addEventListener('submit', function(e) {
            e.preventDefault();
            // Show authentication modal
            document.getElementById('auth-modal').style.display = 'block';
        });

        document.getElementById('modal-cancel').addEventListener('click', function() {
            document.getElementById('auth-modal').style.display = 'none';
        });

        document.getElementById('modal-submit').addEventListener('click', function() {
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirm-password').value;
            
            // Simulate email existence check
            const emailExists = checkEmailExists(email);
            
            if (emailExists) {
                // Log In flow
                document.getElementById('modal-title').textContent = 'Log In';
                document.getElementById('confirm-password-group').style.display = 'none';
            } else {
                // Sign Up flow
                document.getElementById('modal-title').textContent = 'Sign Up';
                document.getElementById('confirm-password-group').style.display = 'block';
            }
        });

        // Placeholder function to simulate email existence check
        function checkEmailExists(email) {
            // In a real implementation, this would check against a database or API
            // For demonstration, return true if email contains '@'
            return email.includes('@');
        }
    </script>
</body>
</html>
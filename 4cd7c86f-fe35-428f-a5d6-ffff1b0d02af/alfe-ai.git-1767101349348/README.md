# Editor helper

This folder contains a simple `editor.html` that redirects to the AlfeCode editor route for this repository name. It was added so tools that look for an editor page in the candidate directory can find and load an editor view.
